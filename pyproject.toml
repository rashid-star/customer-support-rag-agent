[project]
name = "customer-support-agent"
version = "0.1.0"
description = "Customer Support RAG Agent using LangChain and FAISS"
readme = "README.md"
requires-python = ">=3.10"

dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"